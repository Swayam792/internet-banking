server:
  port: 8083

app:
  config:
    keycloak:
      server-url: http://localhost:8080
      realm: swayam-banking-services
      clientId: banking-service-api-client
      client-secret: aRlFZcF5OzFypWnfSPmMN4GnJfVLCur6

spring:
  application:
    name: banking-user-service
  datasource:
    url: jdbc:mysql://localhost:3306/bankingdb
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

info:
  app:
    name: ${spring.application.name}